# TODO до полноценного продакшена

По приоритету. Отложено на потом (июль 2026).

## Критично (безопасность/данные)

1. **HTTPS** — сейчас HTTP only (см. DEPLOY.md). JWT и пароли идут открытым текстом.
   Let's Encrypt в HestiaCP + доработать `deploy/hestia/*.stpl` шаблон. Блокер №1.
2. **Автоматические бэкапы** — скрипт есть в DEPLOY.md, но запускается руками.
   Нужно: cron на сервере + выгрузка вне машины (rclone в облако) + ротация.
3. **Rate limiting на auth** — брутфорс `/login` ничем не ограничен.
   Достаточно slowapi на auth-service (login/register/refresh).

## Важно (пользовательские сценарии)

4. **Восстановление пароля** — «forgot password» отсутствует; забытый пароль = потерянный аккаунт.
   Тянет SMTP-отправку. Туда же (позже) — подтверждение email при регистрации.
5. **Healthchecks в compose** — ни одного `healthcheck:` в трёх compose-файлах.
   Упавший сервис молча отдаёт 502 через gateway.

## Желательно

6. **Алерты** — Prometheus есть, alert rules нет. Минимум: сервис down 5 мин → уведомление
   (alertmanager → Telegram, или лениво: UptimeRobot поверх).
7. **SEO-минимум** — нет robots.txt, sitemap, og-тегов. Хотя бы meta/og в index.html + robots.txt.
8. **Юридика** — политика конфиденциальности/соглашение при регистрации реальных
   пользователей (152-ФЗ, если РФ).

Рекомендуемый порядок: HTTPS → бэкапы по cron → rate limit → password reset.
